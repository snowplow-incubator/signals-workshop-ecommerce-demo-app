{
  "name": "signals-workshop-worker",
  "version": "1.0.0",
  "description": "CloudFlare worker for proxying user attributes API with auth token caching",
  "main": "src/index.ts",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/worker.js --format=esm --target=es2022",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.35.0"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "api",
    "proxy",
    "auth"
  ]
}
